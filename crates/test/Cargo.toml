[package]
name = "trdelnik-test"
version = "0.1.0"
edition = "2018"
authors = ["AckeeBlockchain <hello@ackeeblockchain.com>"]
license = "Apache-2.0"
repository = "https://github.com/Ackee-Blockchain/trdelnik"
description = "Trdelnik proc-macro"

[lib]
proc-macro = true

[dev-dependencies]
macrotest = "1"

[dependencies]
syn = { version = "1.0", default-features = false }
quote = { version = "1.0", default-features = false }
proc-macro2 = { version = "1.0", default-features = false }
darling = "0.13.1"
